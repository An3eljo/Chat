<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ChatMobile"
             x:Name="PageMainPage"
             x:Class="ChatMobile.Views.MainPage">

    <Grid>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Label Text="{Binding Text}" BindingContext="{x:Reference PageMainPage}"></Label>
            <Label Text="{Binding Text0}" BindingContext="{x:Reference PageMainPage}"></Label>
        </StackLayout>
    </Grid>

</ContentPage>
